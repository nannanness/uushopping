<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.uushopping.mapper.StoreMapper">
    <select id="getAllStore" resultType="org.uushopping.pojo.Store">
        select * from store;
    </select>
    <insert id="addStore" parameterType="org.uushopping.pojo.Store">
        insert into store(storeName, storePassword, storePhone, storeEmail, storeIntroduction) values(#{storeName}, #{storePassword}, #{storePhone}, #{storeEmail}, #{storeIntroduction});
    </insert>
</mapper>